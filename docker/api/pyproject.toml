[project]
name = "forest-cover-api"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "scikit-learn",
    "joblib",
    "numpy",
    "pydantic",
    "pandas",
    "boto3",
]
